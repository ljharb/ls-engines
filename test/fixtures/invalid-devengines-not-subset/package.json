{
	"name": "invalid-devengines-not-subset",
	"description": "Non-private package where devEngines.runtime is NOT a subset of engines - should fail with DEV_ENGINES exit code",
	"repository": {},
	"license": "All rights reserved",
	"engines": {
		"node": ">= 18"
	},
	"devEngines": {
		"runtime": {
			"name": "node",
			"version": ">= 10"
		}
	},
	"dependencies": {
		"object-keys": "^1"
	}
}
